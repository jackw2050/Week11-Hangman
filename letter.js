//letter.js should control whether or not a letter appears as a "_" or as itself on-screen.


exports.doit = function(){
	console.log("letter method");
}


/*
exports.setScreenArray = function(wordObject) {
console.log("letter method");

    wordObject.locationArray.forEach(function() {
        wordObject.screenArray[index] = wordObject.currentLetterGuess;
    }) 
    console.log(JSON.stringify(this.screenArray, null, 2));

    return wordObject;
}
*/

// receive game_word object 
/*

2. if present for loop to replace _ with letter (set goodGuess to true) else goodGuess = false
3. add letter to used array
4. remove/ replace letter in alphabet array
5. set goodGuess to true
6. return object




*/
